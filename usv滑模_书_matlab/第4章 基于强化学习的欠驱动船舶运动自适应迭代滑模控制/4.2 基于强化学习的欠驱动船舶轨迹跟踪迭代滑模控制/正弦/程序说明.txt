%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%源自: 沈智鹏 著《船舶运动自适应滑模控制》 2019年科学出版社
%%下载地址www.shenbert.cn/book/shipmotionASMC.html
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
仿真文件：
ship_Track_sin.mdl  %使用Simulink搭建的正弦曲线轨迹跟踪仿真
sfun_ship.m           %S-function模块搭建的欠驱动船舶模型

本节强化学习的部分程序存储文件遗失，读者可参考4.1节和4.3节的程序实现，

基于强化学习的控制参数优化：xe和ye的滑模面反馈参数k15和k25的神经网络以及奖励信号抖阵信号M(t)都未实现！！！！！！！
只实现了横向和纵向迭代滑模控制器的设计
而且simulink将控制律dn直接作为输入应该不妥？？？
因为这样直接n=n0+dn*T(即n=n0+dn*T(T是自变量时间)(T默认1s)，同delta),
就无法体现主机转速n和船舶操纵的耦合，但是直接这样用也可以。。。
这样就是将控制律dn作为2.3.2节船舶推进装置模型，不考虑主机转速n和船舶操纵的耦合
修正：
%类似舵角的舵角操纵特性的转速操作特性为式2.57的d_delta和d_n方程
%正常的ship输入指令应该是舵角和转速，因此应该根据控制器给出的dn带入转速特性求出期望的n输入
%ship后再输出实际的n。。。
%考虑用控制器的dn求出期望Q_E(因为Q_E和油门位置有线性关系)作为ship输入(因为实际n在Q_P中有体现且将实际n反馈到Q_P)，
%然后ship中dn的特性方程不变，不然就无法体现主机转速n和船舶操纵的耦合了！！！！！
%一个dn是控制律，一个dn是实际nd-n产生(Q_E-Q_P)，即先有控制律dn再有nd-n产生的结果状态dn

